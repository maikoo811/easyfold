# `/demo` — Hugging Face Spaces demo

Build artifacts and configuration for the public demo on Hugging Face Spaces (CPU free tier). The demo runs a stripped-down version of the UI against pre-computed example structures, so users can try the experience without GPU access or model weights.

Empty until the demo phase.
