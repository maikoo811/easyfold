# `/scripts` — Dev utilities

One-off and recurring developer scripts: data fetchers (UniProt, PDB), local helpers, fixtures, repo maintenance. Not part of the runtime — never imported by `frontend/` or `backend/`.
