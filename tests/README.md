# `/tests` — Cross-cutting tests

End-to-end and integration tests that exercise the frontend + backend together (and, later, the Modal job path).

Per-layer unit tests live next to the code they cover:

- Frontend unit/component tests: inside `frontend/`
- Backend unit tests: `backend/tests/`
