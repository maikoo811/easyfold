# TASK-<id> — <title>

**Status:** Not started
**Branch:** —
**Started:** —
**Completed:** —

## Context

<What came before. 1-3 sentences. Reference the prior task IDs and any prerequisite artifacts.>

## Goal

<One sentence. The outcome you're chasing, not the steps to get there.>

## Requirements

- <bullets — what must be true when this lands>

## Out of scope

- <bullets — what you'd be tempted to include but shouldn't, with a brief why>

## Acceptance criteria

- [ ] <testable bullets — each one must be verifiable in CI or in <30s manually>

## Approach

- Plan mode? (yes if 3+ steps per CLAUDE.md rule #1)
- Files to reference (paths + 1-line reason)
- Existing patterns to reuse (cite path + symbol)
- New dependencies — list and justify per CLAUDE.md rule #4

## Implementation notes

<Filled during work. Append entries as decisions, surprises, and scope changes happen. The doc is the long-form record; commit messages stay short. Datestamp each entry if multiple sessions touch the task.>

## Learnings

<Filled after completion. One bullet per learning.

Mark `[generalizable]` for items that would apply to *future* tasks regardless of subject — those get copied to `CLAUDE.md → Learning log` with the date.>
